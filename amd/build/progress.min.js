/**
 * Generic active-view progress tracker for protected image/file resources.
 *
 * Video, audio and PDF have dedicated trackers and must not initialise this
 * module, preventing duplicate writes and double-counted time.
 *
 * @module     mod_videoplayer/progress
 * @copyright  2026 Jose Erasmo Moreno Salgado - Elearning Cloud
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("mod_videoplayer/progress",["core/ajax"],(function(Ajax){return{init:function(options){options=options||{};var cmid=parseInt(options.cmid,10)||0,requiredSeconds=Math.max(1,parseInt(options.requiredSeconds,10)||300),interval=Math.max(1e4,parseInt(options.interval,10)||3e4),activeSeconds=Math.max(0,parseFloat(options.initialTimeSpent||options.initialProgress)||0),completed=Boolean(options.completed),lastTick=Date.now(),pageVisible=!document.hidden,saving=!1,saveQueued=!1,saveTimer=null;if(cmid){var updateTime=function(){var now=Date.now();pageVisible&&(activeSeconds+=Math.max(0,(now-lastTick)/1e3)),lastTick=now},save=function(){if(updateTime(),saving)return saveQueued=!0,Promise.resolve();saving=!0;var percentage=Math.max(0,Math.min(100,activeSeconds/requiredSeconds*100)),request={methodname:"mod_videoplayer_save_progress",args:{cmid:cmid,progress:activeSeconds,completed:completed||percentage>=100,completionpercentage:Math.round(100*percentage)/100,lastpage:0,totalpages:0,timespent:Math.round(activeSeconds),lastposition:0,duration:0}};return Ajax.call([request])[0].then((function(response){return completed=Boolean(response&&response.completed),response})).catch((function(error){window.console&&window.console.warn&&window.console.warn("Drive Resource progress save failed.",error)})).then((function(){return saving=!1,saveQueued?(saveQueued=!1,save()):null}))};document.addEventListener("visibilitychange",(function(){updateTime(),(pageVisible=!document.hidden)||save()})),window.addEventListener("pagehide",(function(){save(),saveTimer&&(window.clearInterval(saveTimer),saveTimer=null)})),saveTimer=window.setInterval(save,interval)}}}}));

//# sourceMappingURL=progress.min.js.map